import cart from './assets/cart.svg'

const CartWidget = () => {
    return (
        <div>
            <img src={cart} alt= 'cart' width={50} className='mt-4'/>
            {0}
        </div>
    )
}

export default CartWidget